/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCADIUM_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ESCROW_DISCRIMINATOR = new Uint8Array([
  252, 228, 127, 1, 60, 43, 54, 28,
]);

export function getUpdateEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ESCROW_DISCRIMINATOR,
  );
}

export type UpdateEscrowInstruction<
  TProgram extends string = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountEscrowVaultState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountClient extends string
        ? WritableAccount<TAccountClient>
        : TAccountClient,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountEscrowVaultState extends string
        ? WritableAccount<TAccountEscrowVaultState>
        : TAccountEscrowVaultState,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateEscrowInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: ReadonlyUint8Array;
  fulfilled: boolean;
  agentOwner: Option<Address>;
};

export type UpdateEscrowInstructionDataArgs = {
  jobId: ReadonlyUint8Array;
  fulfilled: boolean;
  agentOwner: OptionOrNullable<Address>;
};

export function getUpdateEscrowInstructionDataEncoder(): Encoder<UpdateEscrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", fixEncoderSize(getBytesEncoder(), 16)],
      ["fulfilled", getBooleanEncoder()],
      ["agentOwner", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ESCROW_DISCRIMINATOR }),
  );
}

export function getUpdateEscrowInstructionDataDecoder(): Decoder<UpdateEscrowInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", fixDecoderSize(getBytesDecoder(), 16)],
    ["fulfilled", getBooleanDecoder()],
    ["agentOwner", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getUpdateEscrowInstructionDataCodec(): Codec<
  UpdateEscrowInstructionDataArgs,
  UpdateEscrowInstructionData
> {
  return combineCodec(
    getUpdateEscrowInstructionDataEncoder(),
    getUpdateEscrowInstructionDataDecoder(),
  );
}

export type UpdateEscrowAsyncInput<
  TAccountAuthority extends string = string,
  TAccountClient extends string = string,
  TAccountConfig extends string = string,
  TAccountEscrowVaultState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  client: Address<TAccountClient>;
  config?: Address<TAccountConfig>;
  escrowVaultState?: Address<TAccountEscrowVaultState>;
  jobId: UpdateEscrowInstructionDataArgs["jobId"];
  fulfilled: UpdateEscrowInstructionDataArgs["fulfilled"];
  agentOwner: UpdateEscrowInstructionDataArgs["agentOwner"];
};

export async function getUpdateEscrowInstructionAsync<
  TAccountAuthority extends string,
  TAccountClient extends string,
  TAccountConfig extends string,
  TAccountEscrowVaultState extends string,
  TProgramAddress extends Address = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
>(
  input: UpdateEscrowAsyncInput<
    TAccountAuthority,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIUM_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    client: { value: input.client ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    escrowVaultState: {
      value: input.escrowVaultState ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.escrowVaultState.value) {
    accounts.escrowVaultState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([101, 115, 99, 114, 111, 119])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "client",
            accounts.client.value,
          ),
        ),
        fixEncoderSize(getBytesEncoder(), 16).encode(
          getNonNullResolvedInstructionInput("jobId", args.jobId),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("client", accounts.client),
      getAccountMeta("config", accounts.config),
      getAccountMeta("escrowVaultState", accounts.escrowVaultState),
    ],
    data: getUpdateEscrowInstructionDataEncoder().encode(
      args as UpdateEscrowInstructionDataArgs,
    ),
    programAddress,
  } as UpdateEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >);
}

export type UpdateEscrowInput<
  TAccountAuthority extends string = string,
  TAccountClient extends string = string,
  TAccountConfig extends string = string,
  TAccountEscrowVaultState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  client: Address<TAccountClient>;
  config: Address<TAccountConfig>;
  escrowVaultState: Address<TAccountEscrowVaultState>;
  jobId: UpdateEscrowInstructionDataArgs["jobId"];
  fulfilled: UpdateEscrowInstructionDataArgs["fulfilled"];
  agentOwner: UpdateEscrowInstructionDataArgs["agentOwner"];
};

export function getUpdateEscrowInstruction<
  TAccountAuthority extends string,
  TAccountClient extends string,
  TAccountConfig extends string,
  TAccountEscrowVaultState extends string,
  TProgramAddress extends Address = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
>(
  input: UpdateEscrowInput<
    TAccountAuthority,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateEscrowInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountClient,
  TAccountConfig,
  TAccountEscrowVaultState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIUM_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    client: { value: input.client ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    escrowVaultState: {
      value: input.escrowVaultState ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("client", accounts.client),
      getAccountMeta("config", accounts.config),
      getAccountMeta("escrowVaultState", accounts.escrowVaultState),
    ],
    data: getUpdateEscrowInstructionDataEncoder().encode(
      args as UpdateEscrowInstructionDataArgs,
    ),
    programAddress,
  } as UpdateEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >);
}

export type ParsedUpdateEscrowInstruction<
  TProgram extends string = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    client: TAccountMetas[1];
    config: TAccountMetas[2];
    escrowVaultState: TAccountMetas[3];
  };
  data: UpdateEscrowInstructionData;
};

export function parseUpdateEscrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      client: getNextAccount(),
      config: getNextAccount(),
      escrowVaultState: getNextAccount(),
    },
    data: getUpdateEscrowInstructionDataDecoder().decode(instruction.data),
  };
}
