/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCADIUM_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const MARK_DISPUTED_DISCRIMINATOR = new Uint8Array([
  136, 86, 152, 120, 3, 21, 223, 251,
]);

export function getMarkDisputedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MARK_DISPUTED_DISCRIMINATOR,
  );
}

export type MarkDisputedInstruction<
  TProgram extends string = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountEscrowVaultState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClient extends string
        ? WritableSignerAccount<TAccountClient> &
            AccountSignerMeta<TAccountClient>
        : TAccountClient,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountEscrowVaultState extends string
        ? WritableAccount<TAccountEscrowVaultState>
        : TAccountEscrowVaultState,
      ...TRemainingAccounts,
    ]
  >;

export type MarkDisputedInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: ReadonlyUint8Array;
};

export type MarkDisputedInstructionDataArgs = { jobId: ReadonlyUint8Array };

export function getMarkDisputedInstructionDataEncoder(): FixedSizeEncoder<MarkDisputedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", fixEncoderSize(getBytesEncoder(), 16)],
    ]),
    (value) => ({ ...value, discriminator: MARK_DISPUTED_DISCRIMINATOR }),
  );
}

export function getMarkDisputedInstructionDataDecoder(): FixedSizeDecoder<MarkDisputedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", fixDecoderSize(getBytesDecoder(), 16)],
  ]);
}

export function getMarkDisputedInstructionDataCodec(): FixedSizeCodec<
  MarkDisputedInstructionDataArgs,
  MarkDisputedInstructionData
> {
  return combineCodec(
    getMarkDisputedInstructionDataEncoder(),
    getMarkDisputedInstructionDataDecoder(),
  );
}

export type MarkDisputedAsyncInput<
  TAccountClient extends string = string,
  TAccountConfig extends string = string,
  TAccountEscrowVaultState extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  config?: Address<TAccountConfig>;
  escrowVaultState?: Address<TAccountEscrowVaultState>;
  jobId: MarkDisputedInstructionDataArgs["jobId"];
};

export async function getMarkDisputedInstructionAsync<
  TAccountClient extends string,
  TAccountConfig extends string,
  TAccountEscrowVaultState extends string,
  TProgramAddress extends Address = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
>(
  input: MarkDisputedAsyncInput<
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MarkDisputedInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIUM_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    escrowVaultState: {
      value: input.escrowVaultState ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.escrowVaultState.value) {
    accounts.escrowVaultState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([101, 115, 99, 114, 111, 119])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "client",
            accounts.client.value,
          ),
        ),
        fixEncoderSize(getBytesEncoder(), 16).encode(
          getNonNullResolvedInstructionInput("jobId", args.jobId),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("client", accounts.client),
      getAccountMeta("config", accounts.config),
      getAccountMeta("escrowVaultState", accounts.escrowVaultState),
    ],
    data: getMarkDisputedInstructionDataEncoder().encode(
      args as MarkDisputedInstructionDataArgs,
    ),
    programAddress,
  } as MarkDisputedInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >);
}

export type MarkDisputedInput<
  TAccountClient extends string = string,
  TAccountConfig extends string = string,
  TAccountEscrowVaultState extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  config: Address<TAccountConfig>;
  escrowVaultState: Address<TAccountEscrowVaultState>;
  jobId: MarkDisputedInstructionDataArgs["jobId"];
};

export function getMarkDisputedInstruction<
  TAccountClient extends string,
  TAccountConfig extends string,
  TAccountEscrowVaultState extends string,
  TProgramAddress extends Address = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
>(
  input: MarkDisputedInput<
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >,
  config?: { programAddress?: TProgramAddress },
): MarkDisputedInstruction<
  TProgramAddress,
  TAccountClient,
  TAccountConfig,
  TAccountEscrowVaultState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIUM_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    escrowVaultState: {
      value: input.escrowVaultState ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("client", accounts.client),
      getAccountMeta("config", accounts.config),
      getAccountMeta("escrowVaultState", accounts.escrowVaultState),
    ],
    data: getMarkDisputedInstructionDataEncoder().encode(
      args as MarkDisputedInstructionDataArgs,
    ),
    programAddress,
  } as MarkDisputedInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountConfig,
    TAccountEscrowVaultState
  >);
}

export type ParsedMarkDisputedInstruction<
  TProgram extends string = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    client: TAccountMetas[0];
    config: TAccountMetas[1];
    escrowVaultState: TAccountMetas[2];
  };
  data: MarkDisputedInstructionData;
};

export function parseMarkDisputedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMarkDisputedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      client: getNextAccount(),
      config: getNextAccount(),
      escrowVaultState: getNextAccount(),
    },
    data: getMarkDisputedInstructionDataDecoder().decode(instruction.data),
  };
}
