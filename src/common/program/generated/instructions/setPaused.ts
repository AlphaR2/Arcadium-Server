/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ARCADIUM_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const SET_PAUSED_DISCRIMINATOR = new Uint8Array([
  91, 60, 125, 192, 176, 225, 166, 218,
]);

export function getSetPausedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_PAUSED_DISCRIMINATOR);
}

export type SetPausedInstruction<
  TProgram extends string = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
  TAccountEmergencyAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEmergencyAdmin extends string
        ? WritableSignerAccount<TAccountEmergencyAdmin> &
            AccountSignerMeta<TAccountEmergencyAdmin>
        : TAccountEmergencyAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetPausedInstructionData = {
  discriminator: ReadonlyUint8Array;
  paused: boolean;
};

export type SetPausedInstructionDataArgs = { paused: boolean };

export function getSetPausedInstructionDataEncoder(): FixedSizeEncoder<SetPausedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["paused", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_PAUSED_DISCRIMINATOR }),
  );
}

export function getSetPausedInstructionDataDecoder(): FixedSizeDecoder<SetPausedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["paused", getBooleanDecoder()],
  ]);
}

export function getSetPausedInstructionDataCodec(): FixedSizeCodec<
  SetPausedInstructionDataArgs,
  SetPausedInstructionData
> {
  return combineCodec(
    getSetPausedInstructionDataEncoder(),
    getSetPausedInstructionDataDecoder(),
  );
}

export type SetPausedAsyncInput<
  TAccountEmergencyAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  emergencyAdmin: TransactionSigner<TAccountEmergencyAdmin>;
  config?: Address<TAccountConfig>;
  paused: SetPausedInstructionDataArgs["paused"];
};

export async function getSetPausedInstructionAsync<
  TAccountEmergencyAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
>(
  input: SetPausedAsyncInput<TAccountEmergencyAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetPausedInstruction<TProgramAddress, TAccountEmergencyAdmin, TAccountConfig>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIUM_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    emergencyAdmin: { value: input.emergencyAdmin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("emergencyAdmin", accounts.emergencyAdmin),
      getAccountMeta("config", accounts.config),
    ],
    data: getSetPausedInstructionDataEncoder().encode(
      args as SetPausedInstructionDataArgs,
    ),
    programAddress,
  } as SetPausedInstruction<
    TProgramAddress,
    TAccountEmergencyAdmin,
    TAccountConfig
  >);
}

export type SetPausedInput<
  TAccountEmergencyAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  emergencyAdmin: TransactionSigner<TAccountEmergencyAdmin>;
  config: Address<TAccountConfig>;
  paused: SetPausedInstructionDataArgs["paused"];
};

export function getSetPausedInstruction<
  TAccountEmergencyAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
>(
  input: SetPausedInput<TAccountEmergencyAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): SetPausedInstruction<
  TProgramAddress,
  TAccountEmergencyAdmin,
  TAccountConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIUM_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    emergencyAdmin: { value: input.emergencyAdmin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("emergencyAdmin", accounts.emergencyAdmin),
      getAccountMeta("config", accounts.config),
    ],
    data: getSetPausedInstructionDataEncoder().encode(
      args as SetPausedInstructionDataArgs,
    ),
    programAddress,
  } as SetPausedInstruction<
    TProgramAddress,
    TAccountEmergencyAdmin,
    TAccountConfig
  >);
}

export type ParsedSetPausedInstruction<
  TProgram extends string = typeof ARCADIUM_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    emergencyAdmin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetPausedInstructionData;
};

export function parseSetPausedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPausedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { emergencyAdmin: getNextAccount(), config: getNextAccount() },
    data: getSetPausedInstructionDataDecoder().decode(instruction.data),
  };
}
